config:
  payload:
    path: "fake_data_payments.csv"
    fields: 
      - "req"

scenarios:
  - name: "5-2. [Payments][findAllOrderByUser] - 100,000VUs"
    flow:
      - loop:
        - get:
            url: "/payments/user"
          cookie:
            authorization: "{{ accessToken }}"