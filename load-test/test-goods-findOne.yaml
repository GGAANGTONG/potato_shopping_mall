config:
  payload:
    path: "fake_data_goods.csv"
    fields: 
      - "goodsId"

scenarios:
  - name: "2-2. [Goods][findOne] - 10,000VUs"
    flow:
        - get:
            url: "/goods/get-one/{{ goodsId }}"